import type { EffectDefinition } from '../interfaces';

export const canopyReflectionPulseEffect: EffectDefinition = {
  token: 'post.canopyReflectionPulse',
  className: 'visual-layer--canopy',
  baseOpacity: 0.22,
  baseSpeed: 0.85,
  category: 'post',
};
