import type { EffectDefinition } from '../interfaces';

export const hexDriftGridEffect: EffectDefinition = {
  token: 'bg.hexDriftGrid',
  className: 'visual-layer--hex',
  baseOpacity: 0.36,
  baseSpeed: 0.85,
  category: 'background',
};
