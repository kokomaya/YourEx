import type { EffectDefinition } from '../interfaces';

export const binaryStreamEffect: EffectDefinition = {
  token: 'bg.binaryStream',
  className: 'visual-layer--binary',
  baseOpacity: 0.48,
  baseSpeed: 1.3,
  category: 'background',
};
